name: mcqs
description: "A new Flutter project."
publish_to: 'none'  # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  # camera: ^0.9.4+10  # Replace with the latest version available
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  intl: ^0.17.0
  http: ^0.13.3
  google_fonts: ^2.0.0
  camera: ^0.10.5+9
  path_provider: ^2.1.2
  path: ^1.8.3
  image_picker: ^1.0.8
  video_player: ^2.8.6
  fijkplayer: ^0.11.0
  # multi_image_picker: ^4.8.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/images/
    
